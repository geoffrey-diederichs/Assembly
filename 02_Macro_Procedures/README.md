# Macro

```
$ nasm -felf64 macro.asm && ld macro.o -o macro
$ ./macro
Hello, what's your name ?
Geoffrey
Goodbye Geoffrey
```

```
$ nasm -felf64 proc.asm && ld proc.o -o proc
$ ./proc
Hello, what's your name ?
Geoffrey
Goodbye Geoffrey
```
